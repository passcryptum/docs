# Passcryptum

Before pushing your local changes to the remote repository:

1. Check changes locally:

   ```shell
   vitepress dev pages
   ```

1. Run Prettier:

   ```shell
   npm run format:fix
   ```
